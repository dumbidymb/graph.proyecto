// resolvers.ts

const resolvers = {
    Query: {
      // Resolvers de consulta
    },
    Mutation: {
      // Resolvers de mutación
    },
    // Otros resolvers
  };
  
  export default resolvers;
  